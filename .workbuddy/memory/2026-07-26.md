# 2026-07-26 — v2.3.7/2.3.8/2.3.9

## v2.3.9（已提交 cfbb21e，tag v2.3.9）
用户需求：RSRQ、SINR、温度也要图形化显示，简单易读。
- **status.js**：signalCard 的 RSRQ/SINR/温度 由纯文本改为 `metricGauge()` 彩色迷你量表（按质量变色 excellent/good/fair/weak + 刻度标签）。
- **network.js**：首屏 hero 三项 RSRP/RSRQ/SINR 由 `metric()` 文本改为 `metricGauge()`+`heroGauge()` 彩色量表；新增温度量表（共4项，grid 改 repeat(4)，720px 降为 2 列）。
- **后端 mt5700m-at**：`print_network_info` 末尾追加 `print_temperature`（AT^CHIPTEMP? 输出 temperature=），network 页首屏零额外往返即可显示温度；render 用 `raw.match(/^temperature=/m)` 解析。
- 阈值：rsrp≥-80/-90/-100；rsrq≥-10/-15/-20；sinr≥20/13/0；temp<45/55/65（越低越好）。

---
（历史）v2.3.7 首页重构 + v2.3.8 运营商中文/HTMLElement修复/SIM增强

## v2.3.7（已提交 30dbd6b，tag v2.3.7）
用户需求：① LTE 邻区也要支持显示 ② 锁频时自动填入选项并应用 ③ 实时读取当前锁定信息 ④ 首页重构对齐上游。
- **实时读取锁频信息（根因修复）**：`collectFreqLock()` 替代 `matchValues()` 收集全部锁频行
- **LTE 邻区主页显示**：后端 radio-diagnostics 增加 AT^MONNC；前端 lockNeighbourSection()
- **锁频自动填入并应用**：cellLockCard 抽取 applyFill()，锁定按钮先填入面板再执行
- **首页模块与 SIM 信息卡**：后端 status 增加 ICCID/IMSI；status.js Module+SIM 卡

## v2.3.8（已提交 1bebd73，tag v2.3.8）
用户新需求（截图+文字）：运营商显示中文+Logo、首页补全QCI/APN/速率、SSB邻区[object HTMLElement]bug、日志优化。
- **修复 [object HTMLElement] bug**：network.js `row()` 和 status.js `infoRow()` 增加 HTMLElement 类型检测。根因是 mcsDetailNode()/signalBar() 返回的 DOM 节点被 E('strong') 包裹后 LuCI 某路径下 toString()
- **运营商中文名 + Logo**：status.js 新增 operatorInfo()（与 network.js 一致），四大运营商映射（移动/联通/电信/广电）+ 内联 SVG Logo；首页 hero 标题栏和 SIM 卡均显示中文名
- **SIM 卡增强**：新增 APN（AT+CGDCONT? CID1）、QCI（AT+CGEQOSRDP=1）、签约速率（DSAMBR down/up Mbps）；标题改为「SIM & Subscription」
- **后端新增**：print_qos()、print_active_apn()，在 status 命令中调用，首页零额外往返

## 关键事实
- status.js 与上游 FAN789 master 逐行几乎一致（仅差多载波 PCell/SCell 展开）
- 推送：SSH 端口 22 可用（openwrtci key）；443 被 GitHub 断连
- 语法校验：node --check 两个 .js + bash -n 后端脚本均通过
